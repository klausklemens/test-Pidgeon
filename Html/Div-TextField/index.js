function load() {
  var textAreas = document.getElementsByClassName('textarea');
  for (i=0;i<textAreas.length;i++) {
    textAreas[i].contentEditable = true;
  }
}

function scan() {
  //var field = document.getElementsByClassName('textarea')[0];
}